# Maia Neural Network Project


### Steps


1. Download `maia-1100.pb.gz` and place in `project/maia/`.
2. Download the Maia UCI engine and place it in the same folder.
3. Install dependencies: `pip install -r requirements.txt`.
4. Train: `python train.py`.
5. Evaluate: `python evaluate_vs_maia.py`.
